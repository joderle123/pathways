{
  "name": "pathways-therapeutic-case-management",
  "version": "2.1.0",
  "description": "Professional therapeutic case management tool for youth workers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint js/ --ext .js",
    "lint:fix": "eslint js/ --ext .js --fix"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "jsdom": "^24.0.0",
    "eslint": "^9.0.0"
  },
  "dependencies": {
    "dompurify": "^3.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
